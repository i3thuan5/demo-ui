const 意傳服務 = function(){};
const self = 意傳服務;

意傳服務.網域 = () => ("https://hts.ithuan.tw/");

意傳服務.語音合成 = ({分詞='', 腔口=''} = {}) => (
  encodeURI(
    `${self.網域()}語音合成?查詢腔口=${腔口}&查詢語句=${分詞}`
  ));

意傳服務.文本直接合成 = ({腔口='', 語句=''} = {}) => (
  encodeURI(
    `${self.網域()}文本直接合成?查詢腔口=${腔口}&查詢語句=${語句}`
  )
);

export default 意傳服務;
